<Window
    x:Class="axion.Views.Modals.CreateEntryModal"
    x:Name="CreateEntryModalWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:modals="clr-namespace:axion.Views.Modals"
    mc:Ignorable="d"
    Title="Create Entry" Height="400" Width="500"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Background="{DynamicResource AppBackground0}"
    Foreground="{DynamicResource AppForeground0}">

    <Window.DataContext>
        <modals:CreateEntryModalViewModel />
    </Window.DataContext>

    <Grid Background="{DynamicResource AppBackground1}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock
            Text="{Binding CurrentStep.Title}"
            FontSize="20" Margin="12" />

        <!-- Step content -->
        <ContentControl
            Grid.Row="1"
            Content="{Binding CurrentStep}" />

        <!-- Navigation -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="12">
            <Button Style="{StaticResource DangerButton}"
                Command="{Binding BackCommand}"
                Content="Back"
                Width="70"
                Margin="4" />

            <Button
                Content="{Binding NextButtonText}"
                Command="{Binding NextCommand}"
                CommandParameter="{Binding ElementName=CreateEntryModalWindow}"
                Width="90"
                Margin="4"/>
        </StackPanel>
    </Grid>
</Window>